def run_document_agent(question: str, verbose: bool = False) -> str:
    """Run a ReAct agent loop to answer `question` using the document store."""
    raise NotImplementedError("ReAct agent not yet implemented")
